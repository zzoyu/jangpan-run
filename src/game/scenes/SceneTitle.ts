import { Scene } from "phaser";

export default class SceneTitle extends Scene {
  constructor() {
    super({ key: SceneTitle.name });
  }

  preload() {
    // this.load
  }

  create() {
    // this.scene.start("SceneTitle");
  }
}
